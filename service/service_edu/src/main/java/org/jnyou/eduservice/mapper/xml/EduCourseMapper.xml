<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jnyou.eduservice.mapper.EduCourseMapper">

    <select id="queryCoursePublishInfo" resultType="org.jnyou.eduservice.entity.vo.CoursePublishVo">
        SELECT ec.id,ec.title,ec.cover,CONVERT(ec.price, DECIMAL(8,2)) AS price,ec.lesson_num as lessonNum,
        ecd.description,et.name as teacherName,es1.title as subjectLevelOne,es2.title as subjectLevelTwo
        FROM edu_course ec LEFT JOIN edu_course_description ecd ON ec.id = ecd.id
        LEFT JOIN edu_teacher et ON ec.teacher_id = et.id
        LEFT JOIN edu_subject es1 ON es1.id = ec.subject_parent_id
        LEFT JOIN edu_subject es2 ON es2.id = ec.subject_id
        WHERE ec.id = #{courseId}
    </select>
</mapper>
